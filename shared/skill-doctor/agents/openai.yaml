interface:
  display_name: "Skill Doctor"
  short_description: "Grade installed skills from real conversation history"
  default_prompt: "Use $skill-doctor to grade my skills against recent conversations."

policy:
  allow_implicit_invocation: true
